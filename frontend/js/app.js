document.querySelectorAll('.adicionar').forEach(button => {
    button.addEventListener('click', () => {
        alert('Funcionalidade de adicionar produto simulada!');
    });
});

document.querySelectorAll('.editar').forEach(button => {
    button.addEventListener('click', () => {
        alert('Funcionalidade de editar produto simulada!');
    });
});

document.querySelectorAll('.remover').forEach(button => {
    button.addEventListener('click', () => {
        alert('Funcionalidade de remover produto simulada!');
    });
});
